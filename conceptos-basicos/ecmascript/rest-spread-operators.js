// Rest
// El operador rest permite a una función aceptar un número indefinido de argumentos como un array.

// const sum = (...numbers) => {
//   let total = 0;
//   numbers.forEach((number) => (total += number));
//   return total;
// };

// console.log(sum(3, 1)); // output: 4
// console.log(sum(3, 1, 6)); // output: 10

// Tambien se usa para crear un nuevo array con los elementos restantes

// const numbers = [1, 2, 3, 4, 5];
// const numbersCopy = [...numbers];
// console.log(numbersCopy);
